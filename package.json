{
  "name": "kanban-hermes-dashboard",
  "private": false,
  "version": "0.1.0",
  "description": "A local React dashboard for a user's Hermes Agent Kanban service",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/nearxos/hermes-kanban-dashboard.git"
  },
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "check": "npm test && npm run build && git diff --check"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "vitest": "latest",
    "jsdom": "latest"
  }
}
