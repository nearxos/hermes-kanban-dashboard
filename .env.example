# Optional Hermes Kanban API origin, for example:
# VITE_HERMES_KANBAN_ORIGIN=http://127.0.0.1:8000
# The dashboard remains fixture-backed when this is unset or unreachable.
VITE_HERMES_KANBAN_ORIGIN=
